<?php

declare(strict_types=1);

namespace {{ namespace }}\Actions;

use Illuminate\Http\JsonResponse;
use Lorisleiva\Actions\Concerns\AsAction;
use {{ namespace }}\Requests\Store{{ model }}Request;
use {{ namespace }}\Services\{{ model }}Service;

final class Store{{ model }}Action
{
    use AsAction;

    public function __construct(
        private readonly {{ model }}Service $service
    ) {}

    public function asController(Store{{ model }}Request $request): JsonResponse
    {
        $item = $this->service->create($request->validated());

        return response()->json($item, 201);
    }
}
